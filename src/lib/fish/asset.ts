/**
 * 어항 그림.
 *
 * 어항과 물고기, 수초와 자갈까지 한 장에 들어 있는 그림을 **그대로** 쓴다.
 * 그림을 고치지 않는 대신, 아래 좌표들로 그 위에 무엇을 얹을지를 정한다.
 *
 * 좌표는 전부 그림 기준 0~1 정규화 값이고, 실측해서 넣었다.
 * 그림을 갈아끼우면 다시 재야 한다.
 */

export const BOWL = {
  src: "/fish/bowl.png",

  /** 그림은 정사각형이다 (1254×1254) */
  square: true,

  /** 유리 어항의 원 — 물의 탁함이나 빛을 이 안에만 얹는다 */
  glass: { cx: 0.496, cy: 0.525, r: 0.428 },

  /** 물고기가 차지하는 영역 — 사라질 때 이 부분만 물에 녹인다 */
  fish: { x0: 0.255, y0: 0.266, x1: 0.793, y1: 0.856 },

  /** 물고기 눈 — 시선의 중심 */
  eye: { x: 0.693, y: 0.522 },

  /** 입 — 말풍선의 꼬리가 향하는 곳 */
  mouth: { x: 0.752, y: 0.548 },

  /**
   * 말풍선이 앉는 자리. 입 바로 위에 붙이면 얼굴을 덮으므로
   * 머리 위 여백을 기준으로 삼는다.
   */
  speakFrom: { x: 0.752, y: 0.44 },

  /** 먹이가 떨어지는 수면의 높이와 가라앉는 바닥 */
  surface: 0.3,
  floor: 0.82,
} as const;
